services:
  - type: web
    name: company-music-player
    env: node
    buildCommand: npm install && bash install-yt-dlp.sh
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18
